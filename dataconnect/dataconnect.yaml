specVersion: "v1"
serviceId: "my-skin-analyzer"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-skin-analyzer-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
